// Réservé pour l'internationalisation arabe (RTL). Non implémenté à
// l'étape 11 par décision explicite — seule la structure est posée pour
// ne pas bloquer une future traduction complète.
import type fr from "./fr";

const ar: typeof fr | Record<string, never> = {};

export default ar;
